services:
  - type: web
    name: apk-analyzer
    env: docker # ここをdockerに変更
    region: oregon
    plan: free
    # Dockerfileを使用する場合、buildCommandやstartCommandは不要
    # Dockerfile内のCMDがstartCommandの代わりになる
